const palette = {
  red: 'red',
  blue: 'blue',
  green: 'green',
  black: 'black',
  white: 'white',
};

const fontSizes = {
  big: '20px',
  medium: '16px',
  smal: '12px',
};

const commonTheme = { palette, fontSizes };

export default commonTheme;
